class Good3 {
    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4};
        int idx = 0;
        while (idx < arr.length) {
            if (arr[idx] / 2 == 0) {
                // faz nada
            } else {
                arr[idx] = arr[idx] + 1;
            }
            idx = idx + 1;
        }

        for (int k = 0; k < arr.length; k = k + 1) {
            // apenas itera
        }
    }
}